<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryLight"
    tools:context="com.emdevsite.todayhist.MainActivity">

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="@dimen/dimen_view_pager_padding"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Begin Swipe hint -->
    <ImageView
        android:id="@+id/iv_left"
        android:src="@drawable/ic_keyboard_arrow_left_black_24dp"
        android:tint="@color/colorHint"
        android:alpha="0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/dimen_arrow_padding"
        android:layout_marginStart="@dimen/dimen_arrow_padding"
        app:layout_constraintTop_toTopOf="@+id/tv_swipe_hint"
        app:layout_constraintBottom_toBottomOf="@+id/tv_swipe_hint"
        app:layout_constraintLeft_toLeftOf="@id/vp_text"/>

    <TextView
        android:id="@+id/tv_swipe_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dimen_swipe_text_view_margin_bottom"
        android:textColor="@color/colorHint"
        android:text="@string/swipe_hint"
        android:alpha="0"
        app:layout_constraintBottom_toBottomOf="@+id/vp_text"
        app:layout_constraintLeft_toRightOf="@id/iv_left"
        app:layout_constraintRight_toLeftOf="@id/iv_right"/>

    <ImageView
        android:id="@+id/iv_right"
        android:src="@drawable/ic_keyboard_arrow_right_black_24dp"
        android:tint="@color/colorHint"
        android:alpha="0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/dimen_arrow_padding"
        android:layout_marginEnd="@dimen/dimen_arrow_padding"
        app:layout_constraintTop_toTopOf="@+id/tv_swipe_hint"
        app:layout_constraintBottom_toBottomOf="@+id/tv_swipe_hint"
        app:layout_constraintRight_toRightOf="@id/vp_text"/>
    <!-- End Swipe hint -->

</android.support.constraint.ConstraintLayout>
