<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/alpha_gradient_radial"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/tv_date"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/padding_viewPager"
        android:layout_marginTop="@dimen/margin_viewPager"
        android:textAlignment="center"
        android:textColor="@color/colorSecondary"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
        app:fontFamily="sans-serif-smallcaps"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="@id/tv_year"
        app:layout_constraintRight_toRightOf="@id/tv_year"
        tools:text="Blah, Blah 14th"/>

    <TextView
        android:id="@+id/tv_year"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/padding_viewPager"
        android:paddingLeft="@dimen/padding_viewPager"
        android:paddingRight="@dimen/padding_viewPager"
        android:paddingStart="@dimen/padding_viewPager"
        android:paddingEnd="@dimen/padding_viewPager"
        android:layout_marginBottom="@dimen/margin_viewPager"
        android:layout_marginLeft="@dimen/margin_viewPager"
        android:layout_marginRight="@dimen/margin_viewPager"
        android:layout_marginStart="@dimen/margin_viewPager"
        android:layout_marginEnd="@dimen/margin_viewPager"
        android:textColor="@color/colorSecondary"
        android:textAlignment="center"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Display1"
        app:fontFamily="sans-serif-smallcaps"
        app:layout_constraintTop_toBottomOf="@+id/tv_date"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="1997"/>

</android.support.constraint.ConstraintLayout>