<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/parchment"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/iv_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <include
        android:id="@+id/v_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        layout="@layout/view_date_display" />

    <TextView
        android:id="@+id/tv_history"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingTop="64dp"
        android:paddingLeft="@dimen/paddingHoriz_event"
        android:paddingRight="@dimen/paddingHoriz_event"
        android:paddingStart="@dimen/paddingHoriz_event"
        android:paddingEnd="@dimen/paddingHoriz_event"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Body2"
        android:textColor="@color/colorPrimaryText"
        android:textAlignment="center"
        android:textSize="@dimen/textSize_event"
        app:fontFamily="sans-serif-smallcaps"
        app:layout_constraintTop_toBottomOf="@+id/v_date"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="@id/v_date"
        app:layout_constraintRight_toRightOf="@id/v_date"
        app:layout_constraintStart_toStartOf="@id/v_date"
        app:layout_constraintEnd_toEndOf="@id/v_date"
        tools:text="Blahblahblahblahblah" />

</android.support.constraint.ConstraintLayout>